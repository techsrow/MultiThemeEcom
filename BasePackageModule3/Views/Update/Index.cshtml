@model BasePackageModule3.PaginatedList<BasePackageModule3.Models.Update>

<section id="header-4">
    <div class="container">
        <div class="row">

            <div class="col-lg-12 col-sm-12">
                <h1 class="text-center" style="color:#cddc39!important;">Update</h1>

            </div>
        </div>

    </div>
</section>

<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
        <div class="col-lg-3 col-sm-12 col-md-3 mt-3">
            @*<div class="card">
                <div class="card-horizontal" style=" display: flex;
    flex: 1 1 auto;">
                    <div class="img-square-wrapper">
                        <img class="" src="@Url.Content(item.Image)" style="width:300px !important;height:180px !important;" alt="Card image cap">
                    </div>
                    <div class="card-body">
                        <span></span>
                        <h4 class="card-title" style="color:#cddc39!important;">@item.Title <span class="badge badge-secondary" style="font-size:50% !important;">@item.CreatedDate.ToLongDateString()</span></h4>
                        <p class="card-text">

                            @if (item.Content.Length <= 94)

                            {
                                @Html.Raw(item.Content)

                            }
                            else
                            {
                                @Html.Raw(item.Content.Substring(0, 94))<text>...</text>
                            }
                        </p>
                        <a style="background-color:#cddc39!important;border:none;" asp-controller="Update" asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm" role="button" aria-disabled="true">See More !</a>

                    </div>
                </div>

            </div>*@
            <div class="card" style="width: 18rem;">
                <img class="card-img-top" src="@Url.Content(item.Image)" alt="Card image cap">
                <div class="card-body">
                    <h5 class="card-title">@item.Title</h5>
                    <p class="card-text" style="color:#cddc39!important;">
                        @if (item.Content.Length <= 94)

                        {
                            @Html.Raw(item.Content)

                        }
                        else
                        {
                            @Html.Raw(item.Content.Substring(0, 94))<text>...</text>
                        }
                    </p>
                    <a style="background-color:#cddc39!important;border:none;" asp-controller="Update" asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm" role="button" aria-disabled="true">See More !</a>
                </div>
            </div>
        </div>
        }
    </div>
</div>

@{
    var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.HasNextPage ? "disabled" : "";
}

<div class="container-fluid">
    <div class="row mb-4 mt-3">
        <div class="col-sm-12 ml">
            <a style="background:#CC6733;border:none;" asp-action="Index"
               asp-route-sortOrder="@ViewData["CurrentSort"]"
               asp-route-pageNumber="@(Model.PageIndex - 1)"
               asp-route-currentFilter="@ViewData["CurrentFilter"]"
               class="btn btn-primary @prevDisabled">
                Previous
            </a>
            <a style="background:#CC6733;border:none;" asp-action="Index"
               asp-route-sortOrder="@ViewData["CurrentSort"]"
               asp-route-pageNumber="@(Model.PageIndex + 1)"
               asp-route-currentFilter="@ViewData["CurrentFilter"]"
               class="btn btn-primary @nextDisabled">
                Next
            </a>
        </div>
    </div>
</div>