
@model BasePackageModule2.ViewModels.HomeViewModel
@if (Model._themeSettings != null)
{
    @foreach (var theme in Model._themeSettings)
    {
        @if (theme.ThemeName == "Furniture1" && theme.ThemeCategory == "Furniture1" && theme.IsActive == true && theme.SkinType == "Furniture1")
        {
            

        }
    }
}